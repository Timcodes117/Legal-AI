# Used by Vite at build/dev time. On deploy, set these on the frontend host/CI.
# Leave VITE_API_BASE empty locally so /api is proxied to FastAPI.
VITE_API_BASE=
VITE_INTRON_WIDGET_KEY=paste_your_widget_key_here
